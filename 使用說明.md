# PBI AI Bridge 使用說明

> 給第一次使用的同事。照順序做，大約 15 分鐘可以跑出第一個結果。
> 想看技術全貌請看 [README.md](README.md)。

**目錄**

1. [這個工具能幫你做什麼](#1-這個工具能幫你做什麼)
2. [事前準備](#2-事前準備)
3. [第一次啟動](#3-第一次啟動)
4. [設定資料保護清單（重要）](#4-設定資料保護清單重要)
5. [每天的使用流程](#5-每天的使用流程)
6. [跟 AI 協作](#6-跟-ai-協作)
7. [AI 會請你幫忙的五件事](#7-ai-會請你幫忙的五件事)
8. [網頁儀表板](#8-網頁儀表板)
9. [資料保護：你會遇到的情況](#9-資料保護你會遇到的情況)
10. [快照與還原](#10-快照與還原)
11. [疑難排解](#11-疑難排解)
12. [附錄：自己用 PowerShell 操作](#12-附錄自己用-powershell-操作)

---

## 1. 這個工具能幫你做什麼

讓 AI 直接操作你**正在開著的** Power BI Desktop 模型，並且**當場驗算**。例如：

- 「新增『毛利率』量值，用產品線驗算一下數字」
- 「檢查模型裡有沒有壞掉的公式、沒用到的欄位、雙向關聯」
- 「把『銷售額』改名成『銷售總額』，所有引用它的公式一起改」
- 「建一個時間智慧計算群組：YTD、MTD、去年同期」
- 「『訂單』表的日期欄是文字，幫我寫轉成日期的 Power Query」

| AI 看得到 | AI 看不到 |
|---|---|
| 資料表、欄位**名稱**、DAX 公式、關聯 | 客戶名稱等身分欄位的**值**、逐筆金額 |

「看不到」由服務強制把關，不是靠 AI 自律。前提是第 4 節的清單要設好。

---

## 2. 事前準備

| ☐ | 項目 | 說明 |
|---|---|---|
| ☐ | Windows 10 / 11 | — |
| ☐ | Power BI Desktop | 能正常開啟你的報表 |
| ☐ | 網路 | 第一次啟動要下載元件。用瀏覽器開 <https://api.nuget.org/v3/index.json> 看得到文字就沒問題 |
| ☐ | Claude Code | Claude 桌面版的 **Code** 分頁，或終端機的 `claude` 指令 |

**.NET SDK 不用先裝**，啟動檔發現沒有會問你要不要直接安裝。

---

## 3. 第一次啟動

### 步驟 1：解壓縮到本機資料夾

把整個資料夾解壓縮到例如 `C:\PBI_AI_Bridge`。

- ❌ 不要在 zip 裡直接點兩下執行 —— 要先**解壓縮**
- ❌ 不要放在 OneDrive 同步資料夾 —— 同步會鎖檔，編譯會隨機失敗
- 💡 如果是從 Email / Teams 下載的 zip：**解壓縮前**在 zip 上按右鍵 → 內容 → 勾選「解除封鎖」→ 確定。可以避免之後 Windows 與 PowerShell 擋下檔案

### 步驟 2：開啟一份 Power BI 檔案

任何 .pbix 或 .pbip 都可以。

### 步驟 3：雙擊 `🚀啟動PBI終極儀表板.bat`

這是**唯一**需要點的檔案。它會依序檢查 5 個項目，缺什麼會停下來問你。第一次大概會看到：

```
[1/5] Files
      OK
[2/5] .NET SDK
      OK                                    ← 沒有的話會問你要不要安裝（見下方）
[3/5] Settings file
      Created, with an API Key made just for this computer.
      IMPORTANT - protect your data before AI queries it
      Open that file in Notepad now? [Y/N]  ← 按 Y，照第 4 節設定
[4/5] Libraries
      First run: 6 libraries must be downloaded from nuget.org, about 17 MB.
      Download them now? [Y/N]              ← 按 Y
      OK
[5/5] Compile
      Compiling, about 30 seconds...
      OK
----------------------------------------------
  Starting - the dashboard opens by itself.
  KEEP THIS WINDOW OPEN. Closing it stops the server.
----------------------------------------------
🛡️ 資料保護啟用 — 身分欄位／樣式 23 條、金額欄位／樣式 11 條、安全白名單 1 條
📂 偵測到 1 個 Power BI 實例：
     · Port 51234  銷售報表.pbix  [PBIX]
🌐 網頁儀表板：http://localhost:5500/
```

接著瀏覽器會自動打開儀表板，看到你的資料表就完成了。**黑窗保持開著**，可以縮到最小。

**不需要抄金鑰、也不需要貼金鑰**，儀表板與 AI 工具會自己讀。

### 如果問你要不要安裝 .NET SDK

```
[2/5] .NET SDK
      Not found. This tool needs the .NET SDK, version 8 or newer.
      It can be installed right here with winget, the installer built into Windows:
        Package   Microsoft .NET SDK 8   id: Microsoft.DotNet.SDK.8
        Source    Microsoft, about 250 MB
      Install it now? [Y/N]
```

- 按 **Y**：直接下載安裝。Windows 可能跳出「是否允許此 App 變更你的裝置」，按「是」。裝好會自動繼續
- 按 **N**，或安裝失敗（例如公司電腦停用了 winget）：畫面會列出手動安裝步驟，然後
  - 按 **O** 開啟下載頁 → 找「SDK 8.0.x」→ Windows **x64** Installer → 安裝（注意是 **SDK**，不是 Runtime）
  - 裝好回到黑窗按 **R** 重新檢查，不用重開
  - 按 **Q** 離開

### 套件下載失敗

第 4 步要從 nuget.org 下載 6 個 Microsoft Analysis Services 相關元件（約 17 MB，清單見 [README](README.md#系統需求)）。失敗時啟動檔會自己判斷原因：

- **這台電腦沒註冊 nuget.org**：會問「Add nuget.org as a package source and try again?」，按 Y 自動加上並重試
- **網路連不到**：畫面會列出檢查步驟，處理好之後按 **R** 重試
  1. 用瀏覽器開 <https://api.nuget.org/v3/index.json>，看得到一段文字就代表連得到
  2. 瀏覽器打得開、黑窗卻還是失敗，通常是公司 Proxy —— 請 IT 協助，或提供公司內部的 NuGet 來源
  3. 有開 VPN 的話，切換一下再試

| 其他畫面 | 代表 | 怎麼辦 |
|---|---|---|
| 「Windows 已保護您的電腦」 | 從網路下載的檔案 | 按「其他資訊」→「仍要執行」 |
| `[WARNING] This folder is inside OneDrive` | 在 OneDrive 裡 | 建議把資料夾搬出來再啟動 |
| `[ERROR] Some files are missing` | 檔案不完整 | 重新取得整個資料夾並解壓縮 |
| `The server is already running in another window` | 已經開過了 | 不是錯誤，會直接打開儀表板 |

---

## 4. 設定資料保護清單（重要）

預設清單只認得英文通用欄位名（`customer`、`amount`…）。
**你的模型如果用中文欄位名，預設清單幾乎擋不到任何東西。** 請在讓 AI 查詢資料之前完成這一步。
第一次啟動時按 Y 會直接用記事本打開；之後要改也可以自己開。

### 要改哪裡

`pbibridge_csharp\appsettings.json` 的 `DataProtection` 段落，在清單**後面加上**你模型的欄位（原本的英文樣式保留）。
下面是示意，`…（原本的保留）…` 那幾行代表原有內容，**不要照打進去**：

```json
"DenyColumns": [
  "*customer*",
  "*client*",
  …（原本的保留）…
  "*description*",
  "*客戶*",
  "*經銷商*",
  "*聯絡人*",
  "*業務員*",
  "*電話*",
  "*地址*",
  "*備註*"
],
"AggregateOnlyColumns": [
  "*amount*",
  …（原本的保留）…
  "*salary*",
  "*金額*",
  "*單價*",
  "*成本*",
  "*毛利*"
],
"AllowColumns": [
  "客戶等級"
],
```

| 清單 | 放什麼 | AI 可以 | AI 不行 |
|---|---|---|---|
| `DenyColumns` | 客戶名、公司名、人名、電話、Email、地址、備註等自由文字 | 數有幾個 | 看到任何一個值 |
| `AggregateOnlyColumns` | 金額、單價、成本、薪資 | 加總、平均、最大最小 | 逐筆列出 |
| `AllowColumns` | 被上面樣式誤傷、其實不敏感的欄位 | 一般使用 | — |

### 寫法規則

- `*` 代表任意文字：`*客戶*` 會比對到「客戶名稱」「終端客戶」「客戶代號」
- 比對時**忽略大小寫、空白、底線、連字號**：`account name`、`Account_Name`、`account-name` 視為同一個
- 誤傷時（例如 `*客戶*` 打到「客戶等級」這種分類欄位），把**那個欄位名**加進 `AllowColumns`，不要刪掉整條樣式
- JSON 格式要正確：每一項用雙引號包起來、項目之間用逗號、**每個清單最後一項後面不能有逗號**

> 不確定模型裡有哪些欄位？打開網頁儀表板（第 8 節）就能看到所有欄位名稱。
> 也可以請 AI：「列出所有欄位名稱，幫我挑出可能是客戶身分或金額的欄位，給我建議的清單」—— 這只會讀欄位名稱，不會讀資料。

### 讓設定生效

- **第一次啟動時**在記事本改完、存檔、關閉記事本，回黑窗按任意鍵繼續即可
- **之後再改**：關掉黑窗 → 重新雙擊 🚀

確認黑窗裡這一行的條數跟你清單的項目數一致：

```
🛡️ 資料保護啟用 — 身分欄位／樣式 23 條、金額欄位／樣式 11 條、安全白名單 1 條
```

如果黑窗出現紅字錯誤並顯示 `[EXIT] Server stopped`，多半是 JSON 格式打錯（逗號、引號），往上捲看錯誤訊息，改好再重開。

### 驗證防護真的有效

Power BI 開著、服務開著的情況下，請 AI 執行 `tools\Test-DataGuard.ps1`，或自己在工具資料夾的 PowerShell 裡跑：

```powershell
.\tools\Test-DataGuard.ps1
```

同時開了多個 Power BI 時，要指定測哪一個。Port 可以在黑窗「偵測到 N 個 Power BI 實例」那段看到：

```powershell
.\tools\Test-DataGuard.ps1 -Target 61853
```

它會拿你模型裡**實際存在**的管制欄位去模擬各種「偷看」手法，**只回報有沒有被擋下，不會印出任何資料**。
最後一行是 `結果：N 通過 / 0 失敗` 就代表有效。

---

## 5. 每天的使用流程

```
① 開啟 Power BI 檔案
② 雙擊 🚀啟動PBI終極儀表板.bat（幾秒鐘，儀表板自動打開；黑窗保持開著）
③ 在工具資料夾開啟 Claude Code，開始對話
④ 做完 → 確認已存檔 → 關掉黑窗
```

- ①② 順序可以對調，服務啟動後才開的 Power BI 也偵測得到
- **黑窗關掉 = 服務停止**，AI 就連不到了
- 找不到儀表板分頁了？**再雙擊一次 🚀** 就好，它發現服務已經在跑，會直接幫你打開儀表板
- 可以同時開好幾個 Power BI 檔案，AI 會問你要改哪一個

---

## 6. 跟 AI 協作

### 開啟方式

- **Claude 桌面版**：Code 分頁 → 選擇資料夾 → 選這個工具的資料夾
- **終端機**：先 `cd C:\PBI_AI_Bridge`，再輸入 `claude`

**一定要在這個資料夾開啟**，AI 才會自動讀到 `CLAUDE.md` 的工作規範（資料保護、防毒相容、驗算習慣都寫在裡面）。
第一次開啟時如果詢問是否信任這個資料夾，選信任。

### 第一句話

> 幫我檢查橋接服務，然後告訴我目前這份模型的結構

AI 會自己載入工具、確認連線、列出開著的 Power BI 與資料表概況。

### 好用的說法

| 你想做 | 可以這樣說 |
|---|---|
| 了解模型 | 「這份模型有哪些事實表跟維度表？關聯怎麼接？」 |
| 健檢 | 「跑一次模型健檢，列出壞掉的公式和可能沒用到的欄位」 |
| 新增量值 | 「新增『毛利率』= [毛利] / [營收]，放在『量值』表，百分比格式，做完用產品線驗算」 |
| 修公式 | 「『年增率』在一月份是空白，幫我找原因並修好」 |
| 改名 | 「把『銷售額』改成『銷售總額』，先給我看會改到哪些公式」 |
| 整理 | 「把所有庫存相關的量值放進『庫存』顯示資料夾」 |
| Power Query | 「『訂單』表的日期欄是 YYYYMMDD 文字，幫我寫轉日期的 M」 |
| 效能 | 「哪幾張表最佔記憶體？」「這個量值很慢，有沒有更快的寫法？」 |
| 多個檔案 | 「我開了兩個 Power BI，這次改『銷售報表』那個」 |

### 好習慣

- **確認改的是哪一份**：同名檔案常有好幾份（桌面一份、專案資料夾一份）。AI 選定時會印出完整路徑，看一眼是不是你要的
- **大改之前說「先存快照」**：AI 通常會自己做，明講更保險
- **要求驗算**：說「做完驗算給我看」，AI 會用彙總查詢確認數字
- **結束前確認存檔**：修改只在記憶體裡，AI 會嘗試存檔；它說「沒存到」時請自己按 Ctrl+S

---

## 7. AI 會請你幫忙的五件事

有些事 AI **刻意不自己做**（為了資料保護，或避免觸發公司防毒），會停下來請你動手。這是正常的。

### ① 決定要不要給「一次性權杖」

AI 的查詢被資料保護擋下、而它認為確實需要看明細時，會先說明**要看哪張表、哪些欄位、幾列、為什麼彙總不夠**。

- **不同意**：直接說「不行，用彙總就好」
- **同意**：到 🚀 黑窗找到剛出現的方框，裡面寫著「若你確認要放行『這一句』查詢，把下列權杖貼給 AI」，把下面那串權杖複製貼給 AI

權杖只對**那一句查詢**有效、10 分鐘過期、用一次就失效，AI 不能拿去查別的。

### ② 把 M 腳本貼進 Power Query

AI 不能直接改 M（技術原因：會讓 Power BI 卡在「查詢中有暫止的變更尚未套用」）。流程是：

1. AI 先用白話說明打算怎麼改，等你說可以
2. AI 給你一份**完整**的 `let … in` 程式碼
3. 你：Power Query 編輯器 → 選那張查詢 → **進階編輯器** → 全選 → 貼上 → 完成 → **關閉並套用**
4. 回覆 AI 以下其中一種就好：
   - `OK`
   - `錯` ＋ 直接貼錯誤訊息（貼之前看一眼有沒有客戶名稱等真實資料）
   - `怪` ＋ 截圖（預覽截圖含真實資料，必要時先遮蔽）

之後 AI 會比對改前改後的列數、空值數，抓出預覽畫面看不到的問題（預覽只顯示前 1000 列）。

### ③ 手動存檔

AI 存檔是模擬按 Ctrl+S。它回報「沒存到」時，切到 Power BI 自己按 Ctrl+S 就好。
（大檔案存檔要時間，有時其實已經存到了，AI 會隔一下再確認。）

### ④ 重開服務

AI 修改伺服器程式或設定後會請你：**關掉黑窗** → 告訴它 → **重新雙擊 🚀**（程式有改會自動重新編譯）→ 告訴它。
AI 被規範不能自己關閉或啟動程式（那是防毒最敏感的行為）。

### ⑤ 回報防毒訊息

如果趨勢科技跳出警報，**先截圖並告訴 AI**，它會說明剛才做了什麼、在什麼時間點。AI 被規範不能自己去讀防毒紀錄。

---

## 8. 網頁儀表板

🚀 啟動後會自動打開 <http://localhost:5500/>。分頁不見了就再雙擊一次 🚀，或直接在瀏覽器輸入這個網址（服務要開著）。

- 瀏覽資料表、欄位、DAX 量值、M 腳本
- 右上角下拉選單切換 Power BI 檔案
- **不需要輸入金鑰**：頁面由服務提供，會自動帶入
- 直接雙擊 `PowerBI_Visualizer.html` 也可以，會自動轉到上面的網址

儀表板**只能瀏覽，不會修改模型**。

---

## 9. 資料保護：你會遇到的情況

| 情況 | 結果 |
|---|---|
| AI 查「有幾個客戶」 | ✅ 回傳數字 |
| AI 查「各產品線營收」 | ✅ 回傳彙總 |
| AI 想列出客戶名稱 | ⛔ 擋下，AI 會改用計數或去敏模式 |
| AI 想逐筆看金額 | ⛔ 擋下，AI 會改用加總 |
| AI 要分析「前 20 大客戶的營收」 | ✅ 用去敏模式，客戶顯示成 `ID_A3F1B2` 這種代號 |
| 查詢結果超過 50 列明細／300 列彙總 | 只回傳到上限，AI 會知道結果被截斷 |

- **代號怎麼對照真名？** 真名只印在 🚀 黑窗 —— 你看得到，AI 看不到。同一個客戶永遠是同一個代號
- **想知道 AI 查了什麼？** 打開 `audit\query-audit-YYYYMM.tsv`，每一句查詢和判定結果都在裡面（不含回傳值）

### 你自己也要注意

- **錯誤訊息可能含真實資料**：Power BI 的轉型錯誤常直接印出出錯的值，貼給 AI 前先看一眼
- **截圖會送進 AI**：含真實資料的畫面先遮蔽
- **條件很窄的彙總等於明細**：「某某公司上個月營收」雖然是加總，其實就是那家公司的數字
- **清單沒列到的欄位不受保護**：模型新增了敏感欄位，記得回來補清單

---

## 10. 快照與還原

AI 在刪除、改名等破壞性操作前會自動存快照。你也可以隨時要求：

> 先存一個快照，標籤叫「改版前」
>
> 列出這份模型的快照
>
> 還原到「改版前」那個快照，只還原量值，先給我看會改什麼

快照存在 `snapshots\<檔名>_<雜湊>\`，每份模型各自獨立，不會還原到別的模型上。

⚠️ 還原**不是**完整的時光機：

- 只還原量值、計算資料行、關聯、共用運算式、M 腳本
- 快照之後**新建的表格、角色不會被刪掉**
- 快照裡沒有的量值與關聯**會被刪掉**（回到當時狀態，不是合併）
- 還原 M 同樣會造成「暫止的變更」問題，所以通常請 AI「只還原量值」

快照只是模型定義的備份，**不能取代存檔**。重要的報表請另外自己備份 .pbix / .pbip。

---

## 11. 疑難排解

| 症狀 | 原因 | 處理 |
|---|---|---|
| 雙擊 .bat 跳「Windows 已保護您的電腦」 | 從網路下載的檔案 | 「其他資訊」→「仍要執行」 |
| 啟動檔找不到 .NET SDK | 沒裝，或只裝了 Runtime | 按 Y 自動安裝；不行就按 O 手動安裝，裝好按 R。見[第 3 節](#如果問你要不要安裝-net-sdk) |
| 啟動檔 4/5 下載失敗 | 連不到 nuget.org | 見[套件下載失敗](#套件下載失敗) |
| `[ERROR] Compile failed`，訊息有 `being used by another process` | 同時開了兩個 🚀 視窗在編譯 | 關掉多的黑窗，再雙擊一次 |
| `Port 5500 is used by a different program` | 別的程式占用 5500 | 關掉它（VS Code 的 Live Server 預設也用 5500）再雙擊 🚀 |
| 黑窗顯示 `[EXIT] Server stopped` | 服務啟動失敗 | 往上捲看錯誤。改過 `appsettings.json` 的話先檢查 JSON 格式（逗號、引號） |
| 儀表板顯示連線失敗 | 服務沒開，或沒開 Power BI 檔案 | 確認黑窗開著、Power BI 有開檔案 |
| 儀表板顯示「金鑰不符」 | 服務重新啟動過 | 按 F5 重新整理頁面 |
| AI 說「找不到正在執行的 Power BI 檔案」 | Power BI 沒開檔 | 開啟 .pbix / .pbip |
| AI 說「有多個 Power BI 實例」 | 開了好幾個檔案，或同一份檔案開了兩次 | 告訴 AI 要改哪一個；同一份開兩次的話，關掉多的那個 Power BI 視窗 |
| AI 說 `fileChanged = false` | 沒存到，或還在寫入 | 自己按 Ctrl+S |
| Power BI 一直顯示「查詢中有暫止的變更尚未套用」 | M 被 API 改過而失步 | 進階編輯器貼上正確的 M →「關閉並套用」 |
| 改了 `appsettings.json` 沒反應 | 服務沒重開 | 關掉黑窗再雙擊 🚀 |
| AI 載入工具時出現一堆 `Unexpected token` 亂碼 | `tools\PBI-Bridge.ps1` 被存成沒有 BOM 的檔案 | 用 VS Code 開啟 → 右下角編碼 → 「以 UTF-8 with BOM 儲存」 |
| 防毒警報 | — | 停下來、截圖、告訴 AI 與 IT |

### PowerShell 無法執行腳本

錯誤訊息類似「因為這個系統上已停用指令碼執行」或「檔案未經數位簽署」。依序嘗試：

1. **解除下載封鎖**（最常見）—— 在工具資料夾開啟 PowerShell，執行：
   ```powershell
   Get-ChildItem .\tools, .\.claude\hooks -Filter *.ps1 | Unblock-File
   ```
2. **只對目前使用者放寬執行原則**：
   ```powershell
   Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
   ```
3. 仍被公司原則（群組原則）鎖住 → 請洽 IT，不要自行想辦法繞過

---

## 12. 附錄：自己用 PowerShell 操作

不透過 AI 也可以直接用。在工具資料夾開 PowerShell：

```powershell
. .\tools\PBI-Bridge.ps1            # 載入（會列出所有可用指令）
Test-PbiBridge                       # 檢查服務與 Power BI

Get-PbiInstances                     # 列出開著的 Power BI
Use-PbiInstance 銷售報表               # 選定目標（檔名片段或 Port）

Get-PbiMeasures | Select-Object Table, Measure, Expression   # 所有量值
Test-PbiModel                        # 健檢

New-PbiSnapshot -Label 改版前
Set-PbiMeasure -Table 量值 -Name 毛利率 -Expression 'DIVIDE([毛利], [營收])' -Format '0.0%'
(Invoke-Dax 'EVALUATE ROW("毛利率", [毛利率])').Rows
Save-PbiModel                        # 看回傳的 fileChanged 是不是 True
```

完整指令清單見 [README.md「能做什麼」](README.md#能做什麼)；每個指令的參數可以用 `Get-Command <指令名> -Syntax` 查詢。
